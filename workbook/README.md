Workbook rules:

\- This folder may contain only anonymised template workbooks.

\- Do not commit live working copies.

\- Do not commit workbooks containing real pupil, staff, SEND, safeguarding, behaviour, report, or assessment data.

\- Any workbook committed here must be checked for hidden sheets, named ranges, comments, metadata, and cached query data.

\- The source Office Scripts, prompts, Power Query, and config should also exist as separate text files elsewhere in the repo.

